# =============================================================================
# Generated by the agent builder. Universal entries only — every Claude Code
# project needs these regardless of stack.
#
# Renamed from `gitignore` to `.gitignore` during output generation.
#
# Stack-specific entries (Node / Python / Rust / Go / web frameworks / Docker /
# databases) are appended on generation from the snippets in
# `docs/tool-sources.md` § "Project .gitignore stack snippets", matching the
# tech stack chosen during the Full-Suite intake.
# =============================================================================

# --- Secrets + local config (NEVER commit) -----------------------------------
.env
.env.*
!.env.example
!.env.template
secrets/
*.pem
*.key
*.p12
*.pfx
credentials.json
service-account*.json

# --- Claude Code per-user / per-machine --------------------------------------
.claude/settings.local.json
.claude/.session-state.json
CLAUDE.local.md
.claude/agents/*.local.md
.claude/skills/*.local/

# --- Operating system --------------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini
ehthumbs.db
$RECYCLE.BIN/

# --- Editors / IDEs ----------------------------------------------------------
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
*.swp
*.swo
*~

# --- Logs --------------------------------------------------------------------
*.log
logs/
log/

# --- Test artifacts (BAB convention: never commit) ---------------------------
playwright-report/
test-results/
screenshots/
e2e-screenshots/
*.lcov
coverage/
.nyc_output/

# --- Generated archives ------------------------------------------------------
*.tgz
*.zip
*.tar
*.tar.gz

# --- OneDrive / Dropbox / iCloud artefacts -----------------------------------
*.icloud
.dropbox
.dropbox.attr

# --- Project research scratch (wave outputs ARE committed; drafts are not) ---
docs/research/_drafts/
docs/research/_scratch/
